import React, { Component } from 'react';

const Statelesscomponent = ({startWidth,name}) => {
	return(
		<div>
		this value is stateless components:{startWidth} 
		<p>name is:{name}</p>
		</div>
		);
}
export default  Statelesscomponent;